
由 William (William-Gao2010) 开发的一款轻量级工具。

## 💡 开发背景
在进行 Python 开发或使用单片机烧录工具时，Windows Defender 经常会误杀相关文件。
为了提高效率，我编写了这个脚本，让用户可以自选磁盘一键加入排除项（白名单）。

## ✨ 功能特性
- **交互式菜单**：自动扫描并列出电脑所有磁盘分区。
- **一键加白**：调用 PowerShell 高效执行排除指令。
- **权限管理**：内置 UAC 提权请求，确保执行成功。

## 🛠️ 技术栈
- **语言**: Python
- **库**: `psutil`, `webbrowser`, `ctypes`
- **封装**: PyInstaller

## ⚠️ 免责声明
本工具仅用于开发测试环境。将磁盘加入白名单会降低系统防护强度，请确保该磁盘内无恶意文件。